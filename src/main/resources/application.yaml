server:
  port: 8080
spring:
  security:
    user:
      name: krazytop
      password: password
  data:
    mongodb:
      tft:
        uri: ${env:MONGO_DATABASE_URL}/TeamfightTactics
      lol:
        uri: ${env:MONGO_DATABASE_URL}/LeagueOfLegends
      riot:
        uri: ${env:MONGO_DATABASE_URL}/Riot
      clash-royal:
        uri: ${env:MONGO_DATABASE_URL}/ClashRoyal
      destiny:
        uri: ${env:MONGO_DATABASE_URL}/Destiny
    web:
      pageable:
        default-page-size: 5